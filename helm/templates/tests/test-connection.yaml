apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cac.fullname" . }}-test-connection"
  labels:
    {{- include "cac.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cac.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
