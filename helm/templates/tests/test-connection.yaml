apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "big-screen.fullname" . }}-test-connection"
  labels:
    {{- include "big-screen.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "big-screen.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
