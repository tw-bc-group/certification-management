import { PartnerLogoPipe } from './partner-logo.pipe';

describe('PartnerLogoPipe', () => {
  it('create an instance', () => {
    const pipe = new PartnerLogoPipe();
    expect(pipe).toBeTruthy();
  });
});
